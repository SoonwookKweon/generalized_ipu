# 작업 지침

`generalized-ipu` 저장소에서 작업할 때 지키는 규약이다. 개별 문서와 충돌하면 [`docs/01_용어와_정의.md`](docs/01_용어와_정의.md)가 우선한다.

---

## 1. 언어와 문체

- 문서, 주석, docstring, 커밋 메시지는 모두 한국어로 쓴다. 식별자와 코드는 영어로 쓴다.
- 종결형은 평서형 현재 시제(`~한다`)로 통일한다. `~합니다`, `~됩니다`는 대화에만 쓴다.
- 음차 표기를 피하고 번역어를 쓴다. 금지 용어 목록은 용어 문서 5절에 있다.

| 사용하지 않을 표현 | 대신 사용할 표현 |
| --- | --- |
| 샘플, 시드 | 표본, 표본 자료 |
| 마진, 마지널 | 주변표, 제약표 |
| 셀 | 제약 열 또는 교차표 셀 |
| 레벨 | 계층 |
| 피처 | 속성 또는 제약 열 |
| 웨이트 | 가중치 |
| 수렴 실패 | 미수렴, 최대 반복 도달 |

- `IPU`, `CSR`, `nnz` 같은 정착된 영문 약어는 그대로 쓰되, 각 문서에서 처음 등장할 때 한 번 풀어 쓴다.
- 원인을 단정하지 않는다. 미수렴은 알고리즘의 실패가 아니라 제약 집합이 실현 불가능할 가능성을 먼저 제시한다.

---

## 2. 용어

- [`docs/01_용어와_정의.md`](docs/01_용어와_정의.md)가 모든 코드, 주석, 문서, 커밋 메시지의 기준이다. 같은 대상을 두 이름으로 부르지 않는다.
- 새 개념을 도입할 때는 **용어 문서에 정의를 먼저 추가한 뒤** 코드와 문서에 사용한다. 코드가 먼저 나가고 용어가 뒤따르는 순서를 만들지 않는다.
- 색인 $i$는 항상 기본 단위, 색인 $k$는 항상 제약 열을 가리킨다. 다른 의미로 재사용하지 않는다.
- 마스크는 **유효한 쪽이 참**이며, 길이는 제약 열의 수 $K$이지 기본 단위의 수 $n$이 아니다.

---

## 3. 문서와 변경 이력의 분리

| 위치 | 서술 대상 | 시제 |
| --- | --- | --- |
| `changelog/` | 무엇이 언제 어떻게 바뀌었는가 | 과거 |
| `docs/` | 현재 무엇인가 | 현재 |
| docstring | 이 모듈이 지금 무엇을 하는가 | 현재 |

- `docs/`와 docstring에 `기존에는 ~였다`, `~로 변경하였다`, `이전 판에서는` 같은 서술을 두지 않는다. 읽는 사람이 과거를 몰라도 현재 상태만으로 이해할 수 있어야 한다.
- 변경 이력은 `changelog/`에서만 관리한다. 파일명은 `YYYY-MM-DD_주제.md`이며, 각 항목은 **대상 / 내용 / 사유 / 영향** 네 요소를 갖춘다. 영향이 없으면 "없음"으로 명기한다.
- 새 이력 파일을 만들면 [`changelog/README.md`](changelog/README.md)의 목록 표에 한 줄을 추가한다.
- 상대 날짜(`어제`, `지난주`)를 쓰지 않고 절대 날짜로 적는다.

---

## 4. docstring 규약

| 대상 | 내용 |
| --- | --- |
| 모듈 | 첫 줄에 한 문장 요약. 그 모듈이 존재하는 이유나 다른 선택지를 배제한 근거가 있으면 본문에 서술한다 |
| 클래스 | 한 줄 요약. 생성자 인자의 의미가 자명하지 않으면 이어서 서술한다 |
| 공개 메서드 | 한 줄 요약. 반환값의 순서, 예외 조건, 계약이 있으면 본문에 명시한다 |
| 비공개 메서드 | 한 줄 요약만 둔다 |

- 코드를 읽으면 알 수 있는 사실을 반복하지 않는다. **왜** 그렇게 하는지, 무엇을 **전제**하는지, 무엇을 **하지 않는지**를 적는다.
- 오해하기 쉬운 계약은 반드시 명시한다. 유효 마스크의 축, 제약 열의 순서, 계층 관계에 따른 분기, 분수 귀속의 가중치 동질성 전제가 그 예이다.
- docstring 안에서는 수식 조판 없이 평문으로 쓴다. 수식이 필요하면 `docs/`에 두고 참조한다.

---

## 5. 코드

- 주변 코드의 주석 밀도, 이름 규칙, 관용을 따른다. 새 양식을 혼자 도입하지 않는다.
- 예외 메시지에는 무엇이 잘못되었는지와 어떤 값이었는지를 함께 담는다. `길이가 다릅니다`가 아니라 `마스크의 길이(12)가 제약 열 수(8)와 다릅니다`처럼 쓴다.
- 검증은 값이 처음 들어오는 지점에서 한다. 잘못된 입력이 여러 단계를 지나 엉뚱한 곳에서 터지게 두지 않는다.
- 제약 열의 순서에는 단일 기준점을 둔다. 블록의 구축 순서, `ConstraintRegistry`의 등록 순서, 목표 배열의 순서가 어긋나면 목표가 엉뚱한 열에 부과된다.
- 희소성을 깨지 않는다. 속성 행렬을 조밀 배열로 바꾸는 연산을 도입하지 않으며, $\varepsilon$ 평활처럼 성분을 더하는 연산은 대상 열에만 국소적으로 적용한다.
- 성능 주장을 실측 없이 코드나 문서에 적지 않는다. 근거가 없으면 [`docs/03_잔여_과제.md`](docs/03_잔여_과제.md)에 남긴다.

---

## 6. 작업 방식

- 요청받은 범위를 그대로 수행한다. 임의로 좁히거나 넓히지 않으며, 범위를 줄이는 판단은 사용자의 몫이다.
- 작업 도중 확인이 필요한 사항이 생기면, 그 답에 의존하지 않는 부분을 먼저 끝낸 뒤 질문한다.
- 변경 후 `pytest`로 전체 시험을 돌리고 결과를 그대로 보고한다. 실패를 요약으로 감추지 않는다.
- 되돌리기 어렵거나 외부로 나가는 작업(원격 반영, 배포, 삭제, 덮어쓰기)은 먼저 확인을 받는다.
- 긴 파일 내용을 셸 히어독으로 넘기지 않는다. 역슬래시가 소실되고 내용이 잘린다. 파일 작성과 편집은 전용 도구를 쓴다.

---

## 7. 저장소 구조

```
generalized_ipu/
├── README.md               목적, 핵심 개념 요약, 설치와 시험, 문서 작성 규약
├── CLAUDE.md               이 문서
├── pyproject.toml          패키지 정의와 의존성
│
├── docs/                   현재 상태의 서술
│   ├── 01_용어와_정의.md      모든 용어·기호·표기 규약의 기준 문서
│   ├── 02_아키텍처_개요.md    패키지 구성, 자료 흐름, 설계 결정, 사용 예시
│   ├── 03_잔여_과제.md        아직 구현되지 않은 항목과 그 판단 근거
│   └── modules/            패키지별 상세 설명서 6종
│
├── design/                 최초 구상 문서. 용어 문서에 종속된다
├── changelog/              변경 이력. README.md에 목록 표를 유지한다
├── examples/               실행 결과가 포함된 사용 예제 노트북
├── tests/                  시험. conftest.py에 공용 픽스처를 둔다
│
└── src/generalized_ipu/
    ├── hierarchy/          계층 트리, 범주 위계, 집계 사상
    ├── constraints/        제약 정의, 목표 구간, 영 셀과 결측 셀 처리
    ├── matrix/             속성 행렬 구축과 희소 연산 커널
    ├── engine/             반복 루프, 경계 제어, 수렴 판정
    ├── diagnostics/        반복 이력 수집과 진단 보고
    └── io/                 자료 적재와 결과 내보내기
```

**문서 사이의 종속 관계.** `docs/01_용어와_정의.md`가 최상위 기준이며 `design/00_초기구상.md`는 여기에 종속된다. 정의가 충돌하면 용어 문서가 우선한다.

**패키지 의존 방향.** 한쪽으로만 흐른다.

- `hierarchy`와 `constraints`는 다른 내부 패키지에 의존하지 않는다.
- `matrix`는 `hierarchy`에 의존한다.
- `engine`은 `matrix`, `constraints`, `diagnostics`에 의존한다. 갱신 비율의 규칙은 `constraints`가 정의하므로 엔진이 이를 복제하지 않는다.
- `diagnostics`는 배열과 표만 입력으로 받으며 어떤 내부 패키지에도 의존하지 않는다.
- `io`는 최상위 조립 지점으로서 모든 패키지를 사용할 수 있다.

새 패키지를 만들면 모듈 설명서 첫머리에 **책임 범위**와 **책임 밖**을 명시하고, 각 하위 폴더의 `__init__.py`에 공개 API를 모은다.

---

## 8. 예제 노트북

- 예제 자료는 노트북 안에서 생성한다. 별도의 자료 파일을 저장소에 두지 않는다.
- 난수는 씨앗을 고정하여 결과를 재현 가능하게 한다.
- 노트북은 파일을 기록하지 않는다. 저장 절차는 코드 블록으로 보이기만 하고 실행하지 않는다.
- 서술로만 주장하지 않는다. "이렇게 하면 이런 결과가 나온다"고 적었으면 그 결과를 계산하여 대조한다.
- 확인한 사실은 `check()`로 기록한다. 조건이 거짓이면 그 자리에서 `AssertionError`가 발생하므로, 노트북이 끝까지 실행되었다는 것이 곧 모든 검증을 통과했다는 뜻이다.
- 실행 결과를 포함한 상태로 보관한다. 셀을 고쳤으면 전체를 다시 실행하여 출력을 갱신한다.
